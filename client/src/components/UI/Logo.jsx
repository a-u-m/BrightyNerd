import React from "react";

const Logo = (props) => {
  return (
    <React.Fragment>
      <p className={"font-PoorOne " + props.classes}>BrightyNerd</p>
    </React.Fragment>
  );
};

export default Logo;
